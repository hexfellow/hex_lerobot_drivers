# lerobot_camera_berxel

LeRobot camera plugin for Berxel devices.

## Installation

```bash
pip install lerobot_camera_berxel
```

## Usage

Use with `--camera.type=berxel` in lerobot commands, or configure in robot cameras dict.
